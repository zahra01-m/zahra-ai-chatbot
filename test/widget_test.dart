import 'package:flutter_test/flutter_test.dart';
import 'package:zahrachatbot/main.dart';

void main() {
  testWidgets('App loads', (WidgetTester tester) async {
    // Basic test to ensure it compiles
    expect(true, true);
  });
}
